{
  "name": "hitman-cricket",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:browser": "node scripts/browser-check.mjs && node scripts/innings-check.mjs"
  },
  "dependencies": { "three": "^0.180.0" },
  "devDependencies": { "@types/three": "^0.180.0", "typescript": "~5.9.3", "vite": "^7.1.7", "vitest": "^3.2.4", "@playwright/test": "^1.55.1" }
}
